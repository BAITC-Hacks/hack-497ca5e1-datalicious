# Интерфейс — разработчик 2

Сейчас есть только стартовый `ProjectShell`. Добавить выбор пяти решений,
контроль бюджета, ошибки по общим контрактам, результаты до/после и Recharts.
Интерактивные компоненты и графики помечать `"use client"`.
Графики получают готовые результаты из домена; формулу не дублировать.
До интеграции использовать явно помеченные тестовые фикстуры.
OpenAI и `src/server` здесь не импортировать. AI доступен через `/api/analyze`.
