# Server only. The scaffold and its tests run without these values.
# Copy this file to .env.local when implementing AI analysis.
OPENAI_API_KEY=
# Set an available model explicitly; no default model is assumed.
OPENAI_MODEL=
